#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if c is uppercase
 *
 * Return: i=1 i=0
 */
int _isupper(int c);
{
	int i;
	
	if ((c <= "A") && (c >= "Z"))



		i = 1
	else 
		i = 0;
	return (i);
}
